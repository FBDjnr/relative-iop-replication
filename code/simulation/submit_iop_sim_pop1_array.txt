#!/bin/tcsh

#$ -M fbilsond@nd.edu	 # Email address for job notification
#$ -m abe		 # Send mail when job begins, ends and aborts
#$ -pe smp 64		 # Parallel environment + cores PER TASK, smp (1,2,..,128) or mpi-128 (multiple of 128)
#$ -q long@@coba         # Specify queue (long or debug)
#$ -N iop_sim_pop1_array # Specify job name
#$ -t 25-750:25          # Array: tasks 25,50,75,...,750 -> SGE_TASK_ID IS the NC value
#$ -tc 5                 # (optional) cap concurrent tasks to limit cores in flight

module load R/4.5.2     # Required modules

# SGE_TASK_ID is the NC value itself (the grid is defined only by -t above), so
# the R script reads it directly and the per-task console log is named by NC.
R CMD BATCH --no-restore --no-save iop_sim_pop1_array.R iop_sim_pop1_array_nc${SGE_TASK_ID}.out
